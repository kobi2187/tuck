# Fibonacci sequence, first 10 terms, iterative
import io

fn main() -> void [io]:
  var a = 0
  var b = 1
  for i in 0 ..< 10:
    {value: a} toStr printLine
    let next = a + b
    a = b
    b = next
  return
