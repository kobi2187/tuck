# Average of a fixed-size list of numbers
import io

pending:
  fn average({items: Seq[int]}) -> float

fn main() -> void [io]:
  let xs = [3, 1, 4, 1, 5, 9]
  let avg = {items: xs} average
  {value: avg} toStr printLine
  return
