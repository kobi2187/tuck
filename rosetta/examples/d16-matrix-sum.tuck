# Sum all elements of a 2D matrix
import io
import seq

fn main() -> void [io]:
  let m = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
  var total = 0
  for row in m:
    for n in row:
      total = total + n
  {value: total} toStr printLine
  return
