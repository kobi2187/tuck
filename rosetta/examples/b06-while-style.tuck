# While-style loop using Tuck's `for cond:` form
import io

fn main() -> void [io]:
  var n = 100
  var steps = 0
  for n > 1:
    if n % 2 == 0:
      n = n / 2
    else:
      n = n * 3 + 1
    steps += 1
  {value: steps} toStr printLine
  return
